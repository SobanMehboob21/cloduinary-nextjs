apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: production
  labels:
    app: cloudinary-next
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: cloudinary-next
  ports:
  - name: http
    protocol: TCP
    port: 5000
    targetPort: 5000